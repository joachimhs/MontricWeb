<span {{bind-attr class=isSelected:selected:not-selected}} {{action "doSelect"}}>
    {{#if isSelected}}
        <i class="fa fa-check-square-o"></i> {{selectedText}}
    {{else}}
        <i class="fa fa-square-o"></i> {{deselectedText}}
    {{/if}}
</span>
